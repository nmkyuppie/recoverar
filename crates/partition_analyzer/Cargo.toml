[package]
name = "partition_analyzer"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true

[dependencies]
recovery_core = { path = "../recovery_core" }
disk_io = { path = "../disk_io" }
crc32fast = "1.3"
thiserror = "1.0"
byteorder = "1.5"

[dev-dependencies]
tempfile = "3.8"
